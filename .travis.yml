language: java
addons:
  postgresql: "9.4"
jdk:
  - oraclejdk8
services:
  - postgresql
before_script:
  - psql -c 'CREATE USER mydrive WITH PASSWORD 'mydrive';' -U postgres
  - psql -c 'CREATE DATABASE mydrive WITH OWNER=mydrive
                                               LC_COLLATE='en_US.utf8'
                                               LC_CTYPE='en_US.utf8'
                                               ENCODING='UTF8'
                                               TEMPLATE=template0;' -U postgres